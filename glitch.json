{
  "install": "cd backend && pip install -r requirements.txt",
  "start": "cd backend && python -c 'from app.core.database import Base, engine; Base.metadata.create_all(bind=engine)' && python seed_data.py && uvicorn app.main:app --host 0.0.0.0 --port 3000",
  "watch": {
    "restart": {
      "include": [
        "backend/**/*"
      ]
    }
  }
}
